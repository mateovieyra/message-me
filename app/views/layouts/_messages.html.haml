- flash.each do |name, msg|
  %div{class: "mt-3 alert alert-#{name == 'success' ? 'success' : 'danger'} alert-dismissible fade show", role: "alert"}
    = msg
    %button.btn-close{"aria-label" => "Close", "data-bs-dismiss" => "alert", type: "button"}
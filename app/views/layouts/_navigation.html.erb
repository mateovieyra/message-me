<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
<div class="container">
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <li class="nav-item">
        <%= link_to "Chatroom", root_path, class: "nav-link" %>
      </li>
    </ul>
    <% if logged_in? %>
      <%= link_to "Log out", logout_path, class: "btn btn-danger me-2", data: {turbo_method: "delete"}%>
    <% else %>
      <%= link_to "Log in", login_path, class: "btn btn-primary me-2" %>
      <%= link_to "Sign up", login_path, class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
</nav>